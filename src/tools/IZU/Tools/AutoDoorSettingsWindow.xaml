<Window
    x:Class="OHTC.Tools.AutoDoorSettingsWindow"
    x:Name="Window"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:local="clr-namespace:OHTC.Tools"
    xmlns:sc="clr-namespace:SamplesCommon"
    mc:Ignorable="d"
    Title="Auto Door Settings"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Width="600"
    Height="420"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource SystemControlPageBackgroundChromeMediumLowBrush}"
    ui:WindowHelper.UseModernWindowStyle="True"
    ui:TitleBar.ExtendViewIntoTitleBar="True"
    ui:TitleBar.IsBackButtonVisible="True"
    ui:TitleBar.Style="{DynamicResource AppTitleBarStyle}"
    ui:TitleBar.ButtonStyle="{DynamicResource AppTitleBarButtonStyle}"
    ui:TitleBar.BackButtonStyle="{DynamicResource AppTitleBarBackButtonStyle}">

    <Window.Resources>
        <sc:PixelsToGridLengthConverter x:Key="PixelsToGridLength" />

        <Style x:Key="AppTitleBarStyle" TargetType="ui:TitleBarControl">
            <Setter Property="ui:ThemeManager.RequestedTheme" Value="Dark" />
        </Style>
        <Style x:Key="AppTitleBarButtonStyle" TargetType="ui:TitleBarButton">
            <Setter Property="IsActive" Value="{Binding IsActive, ElementName=Window}" />
        </Style>
        <Style x:Key="AppTitleBarBackButtonStyle" TargetType="ui:TitleBarButton" BasedOn="{StaticResource TitleBarBackButtonStyle}">
            <Setter Property="IsActive" Value="{Binding IsActive, ElementName=Window}" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid
            x:Name="AppTitleBar"
            Background="{DynamicResource SystemControlBackgroundAccentBrush}"
            Height="{Binding ElementName=Window, Path=(ui:TitleBar.Height)}"
            ui:ThemeManager.RequestedTheme="Dark" VerticalAlignment="Top">

            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="TextElement.Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsActive, ElementName=Window}" Value="False">
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource SystemControlDisabledBaseMediumLowBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ElementName=Window, Path=(ui:TitleBar.SystemOverlayLeftInset), Converter={StaticResource PixelsToGridLength}}" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{Binding ElementName=Window, Path=(ui:TitleBar.SystemOverlayRightInset), Converter={StaticResource PixelsToGridLength}}" />
            </Grid.ColumnDefinitions>

            <!-- Horizontally centered title -->
            <TextBlock
                Grid.Column="0"
                Grid.ColumnSpan="5"
                FontSize="13"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="{Binding ElementName=Window, Path=Title}" />

        </Grid>

        <Grid Grid.Row="1" Margin="10,20,10,10">
            <ui:SimpleStackPanel>
                <DockPanel Margin="0,20,0,0" IsEnabled="True">
                    <TextBlock Text="" FontSize="14" VerticalAlignment="Center" Margin="0,0,20,0" Width="150" TextAlignment="Right"/>
                    <TextBlock Text="Option" FontSize="14" VerticalAlignment="Center" Margin="10,0,10,0" Width="95"/>
                    <TextBlock Text="Remote" FontSize="14" VerticalAlignment="Center" Margin="0,0,20,0" Width="95" HorizontalAlignment="Left"/>
                </DockPanel>
                <DockPanel Margin="0,10,0,0">
                    <TextBlock Text="Speed (Auto Mode)" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" Width="150" TextAlignment="Right"/>
                    <TextBox x:Name="tb_speed_auto" ui:ControlHelper.PlaceholderText="1.00" Tag="Max 400 mm/s" Width="95" HorizontalAlignment="Left"/>
                    <TextBox IsReadOnly="True" Text="{Binding SpeedAuto,Mode=OneWay}" Width="95" Margin="10,0,0,0"/>
                    <TextBlock Text="mm/s (1~400)" FontSize="14" VerticalAlignment="Center"  Margin="10,0,10,0" Width="110"/>
                    <Button Content=" Set " DockPanel.Dock="Right" VerticalAlignment="Bottom" Margin="10,0,0,0" Width="80" Click="SetAutoSpeed"/>
                </DockPanel>
                <DockPanel Margin="0,20,0,0">
                    <TextBlock Text="Jog Speed" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" Width="150" TextAlignment="Right"/>
                    <TextBox x:Name="tb_speed_jog" ui:ControlHelper.PlaceholderText="1.00" Tag="Max 100 mm/s" Width="95" HorizontalAlignment="Left"/>
                    <TextBox IsReadOnly="True" Text="{Binding SpeedJog,Mode=OneWay}" Width="95" Margin="10,0,0,0"/>
                    <TextBlock Text="mm/s (1~400)" FontSize="14" VerticalAlignment="Center"  Margin="10,0,10,0" Width="110"/>
                    <Button Content=" Set " DockPanel.Dock="Right" VerticalAlignment="Bottom" Margin="10,0,0,0" Width="80" Click="SetJogSpeed"/>
                </DockPanel>
                <DockPanel Margin="0,20,0,0">
                    <TextBlock Text="Open Position" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" Width="150" TextAlignment="Right"/>
                    <TextBox x:Name="tb_open_pos" ui:ControlHelper.PlaceholderText="1.00" Tag="mm" Width="95" HorizontalAlignment="Left"/>
                    <TextBox IsReadOnly="True" Text="{Binding PositionOpened,Mode=OneWay}" Width="95" Margin="10,0,0,0"/>
                    <TextBlock Text="mm" FontSize="14" VerticalAlignment="Center"  Margin="10,0,10,0" Width="110"/>
                    <Button Content=" Set " DockPanel.Dock="Right" VerticalAlignment="Bottom" Margin="10,0,0,0" Width="80" Click="SetOpenPos"/>
                </DockPanel>
                <DockPanel Margin="0,20,0,0">
                    <TextBlock Text="Close Position" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" Width="150" TextAlignment="Right"/>
                    <TextBox x:Name="tb_close_pos" ui:ControlHelper.PlaceholderText="1.00" Tag="mm" Width="95" HorizontalAlignment="Left"/>
                    <TextBox IsReadOnly="True" Text="{Binding PositionClosed,Mode=OneWay}" Width="95" Margin="10,0,0,0"/>
                    <TextBlock Text="mm" FontSize="14" VerticalAlignment="Center" Margin="10,0,10,0" Width="110"/>
                    <Button Content=" Set " DockPanel.Dock="Right" Margin="10,0,0,0" Width="80" Click="SetClosePos"/>
                </DockPanel>
                <DockPanel Margin="0,20,0,0">
                    <TextBlock Text="Current Postion" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0" Width="150" TextAlignment="Right"/>
                    <TextBox  IsReadOnly="True" Text="{Binding PositionCurrent,Mode=OneWay}" Tag="mm" Width="200" HorizontalAlignment="Left"/>
                    <TextBlock Text="mm" FontSize="14" VerticalAlignment="Center" Margin="10,0,10,0" Width="110"/>
                    <Button Content=" Set " DockPanel.Dock="Right" Margin="10,0,0,0" Width="80" Visibility="Collapsed"/>
                </DockPanel>
                <DockPanel Margin="140,20,0,0">
                    <Button Content=" Jog Open " VerticalAlignment="Center" PreviewMouseLeftButtonDown="JogOpen_MouseDown" PreviewMouseLeftButtonUp="JogOpen_MouseUp" Margin="22,0,0,0"/>
                    <Button Content=" Jog Close " VerticalAlignment="Center" PreviewMouseLeftButtonDown="JogClose_MouseDown" PreviewMouseLeftButtonUp="JogClose_MouseUp" Margin="15,0,0,0"/>
                </DockPanel>
            </ui:SimpleStackPanel>
        </Grid>
    </Grid>
</Window>
