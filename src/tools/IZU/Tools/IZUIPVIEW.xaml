<UserControl x:Class="OHTC.Tools.Tools.IZUIPVIEW"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OHTC.Tools">
    <Grid DataContext="{Binding IZU}">
        <Grid.Resources>
            <local:TrueColorConverter x:Key="TrueColorConverter"/>
        </Grid.Resources>
        <DockPanel>
            <TextBlock x:Name="ip">
                <Run Text="{Binding IPAddress}"/>
                <!--<Run Text="("/>
                <Run Text="{Binding Name}"/>
                <Run Text=")"/>-->
            </TextBlock>
            <Ellipse Height="10" Width="10" Fill="{Binding IsHeartBeat,Converter={StaticResource TrueColorConverter},ConverterParameter=flash}"
          Stroke="#99424242" StrokeThickness="1" Margin="2,0,0,0" ToolTip="Connection" ToolTipService.BetweenShowDelay="100" ToolTipService.InitialShowDelay="10"/>
        </DockPanel>
    </Grid>
</UserControl>
