<Window
    x:Class="OHTC.Tools.TransferCommandEditor"
    x:Name="Window"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:local="clr-namespace:OHTC.Tools"
    xmlns:sc="clr-namespace:SamplesCommon"
    mc:Ignorable="d"
    Title="Transfer Commands"
    Width="500"
    Height="500"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False"
    Background="{DynamicResource SystemControlPageBackgroundChromeMediumLowBrush}"
    ui:WindowHelper.UseModernWindowStyle="True"
    ui:TitleBar.ExtendViewIntoTitleBar="True"
    ui:TitleBar.IsBackButtonVisible="True"
    ui:TitleBar.Style="{DynamicResource AppTitleBarStyle}"
    ui:TitleBar.ButtonStyle="{DynamicResource AppTitleBarButtonStyle}"
    ui:TitleBar.BackButtonStyle="{DynamicResource AppTitleBarBackButtonStyle}">

    <Window.Resources>
        <sc:PixelsToGridLengthConverter x:Key="PixelsToGridLength" />

        <Style x:Key="AppTitleBarStyle" TargetType="ui:TitleBarControl">
            <Setter Property="ui:ThemeManager.RequestedTheme" Value="Dark" />
        </Style>
        <Style x:Key="AppTitleBarButtonStyle" TargetType="ui:TitleBarButton">
            <Setter Property="IsActive" Value="{Binding IsActive, ElementName=Window}" />
        </Style>
        <Style x:Key="AppTitleBarBackButtonStyle" TargetType="ui:TitleBarButton" BasedOn="{StaticResource TitleBarBackButtonStyle}">
            <Setter Property="IsActive" Value="{Binding IsActive, ElementName=Window}" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid
            x:Name="AppTitleBar"
            Background="{DynamicResource SystemControlBackgroundAccentBrush}"
            Height="{Binding ElementName=Window, Path=(ui:TitleBar.Height)}"
            ui:ThemeManager.RequestedTheme="Dark">

            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="TextElement.Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsActive, ElementName=Window}" Value="False">
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource SystemControlDisabledBaseMediumLowBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ElementName=Window, Path=(ui:TitleBar.SystemOverlayLeftInset), Converter={StaticResource PixelsToGridLength}}" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="{Binding ElementName=Window, Path=(ui:TitleBar.SystemOverlayRightInset), Converter={StaticResource PixelsToGridLength}}" />
            </Grid.ColumnDefinitions>

            <!-- Horizontally centered title -->
            <TextBlock
                Grid.Column="0"
                Grid.ColumnSpan="5"
                FontSize="13"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="{Binding ElementName=Window, Path=Title}" />

        </Grid>
        
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid>
                <ListBox x:Name="list" VerticalContentAlignment="Center" Width="120" SelectionMode="Single" SelectionChanged="list_SelectionChanged"/>
            </Grid>
            <Grid DataContext="{Binding ElementName=list,Path=SelectedItem}" Grid.Column="2" TextElement.FontSize="16">
                <StackPanel>
                    <TextBlock Text="OHT ID" Style="{StaticResource BaseTextBlockStyle}" Margin="0,0,0,10" />
                    <TextBox x:Name="f_oht" Text="{Binding OHTId}" Margin="0,0"/>
                    <TextBlock Text="Mode" Style="{StaticResource BaseTextBlockStyle}" Margin="0,10,0,10" />
                    <DockPanel Margin="0,0">
                        <RadioButton x:Name="f_move" Content="Move" GroupName="mode"/>
                        <RadioButton x:Name="f_carry" Content="Carry" GroupName="mode"/>
                    </DockPanel>
                    <TextBlock Text="Tasks" Style="{StaticResource BaseTextBlockStyle}" Margin="0,10,0,10" />
                    <TextBox x:Name="f_tasks" MinHeight="200" AcceptsReturn="True"/>
                </StackPanel>
            </Grid>
            <ui:AppBarButton Label="Delete" Grid.ColumnSpan="3" HorizontalAlignment="Left" VerticalAlignment="Bottom" Click="Save_Click" Margin="0,10,0,0">
                <ui:AppBarButton.Icon>
                    <ui:FontIcon Glyph="&#xE107;" />
                </ui:AppBarButton.Icon>
            </ui:AppBarButton>
            <ui:AppBarButton Label="Save" Grid.ColumnSpan="3" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Save_Click" Margin="0,10,0,0">
                <ui:AppBarButton.Icon>
                    <ui:FontIcon Glyph="&#xE105;" />
                </ui:AppBarButton.Icon>
            </ui:AppBarButton>
        </Grid>
    </Grid>
</Window>
