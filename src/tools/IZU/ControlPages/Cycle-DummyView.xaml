<ui:Page
    x:Class="OHTC.Tools.ControlPages.DummyCycleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:app="clr-namespace:OHTC.Tools"
    xmlns:sc="clr-namespace:SamplesCommon"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="800">
    <Page.Resources>
        <app:BooleanReverseConverter x:Key="BooleanReverseConverter"/>
    </Page.Resources>
    <Grid Margin="10" >
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,0">
                <TextBlock Text="OHTC IP" FontSize="14" Padding="0,6,0,0" Margin="5,0,0,0"/>
                <TextBox x:Name="tb_ip" Width="150" Margin="5,0,0,0"/>
                <TextBlock Text="OHT" FontSize="14" Padding="0,6,0,0" Margin="5,0,0,0"/>
                <TextBox x:Name="tb_ohtid" Width="50" Margin="5,0,0,0"/>
                <TextBlock Text="From" FontSize="14" Padding="0,6,0,0" Margin="5,0,0,0"/>
                <TextBox x:Name="tb_point_from" Width="50" Margin="5,0,0,0"/>
                <TextBlock Text="To" FontSize="14" Padding="0,6,0,0" Margin="5,0,0,0"/>
                <TextBox x:Name="tb_point_to" Width="50" Margin="5,0,0,0"/>
                <TextBlock Text="Duration" FontSize="14" Padding="0,6,0,0" Margin="5,0,0,0"/>
                <TextBox x:Name="tb_sec" Width="50" Margin="5,0,0,0"/>
                <TextBlock Text="s" FontSize="14" Padding="0,6,0,0" Margin="5,0,0,0"/>
                <Button Content=" Add " Margin="5,0,0,0" Click="Add_Click"/>
            </DockPanel>
            <DockPanel DataContext="{Binding CurrentCommand}" DockPanel.Dock="Top" Margin="0,10,0,10">
                <Button Content="Start" Click="Go_Click" Width="100" HorizontalAlignment="Left" IsEnabled="{Binding IsRunning, Converter={StaticResource BooleanReverseConverter}}"/>
                <Button DataContext="{Binding CurrentCommand}" Content="Stop" Click="Cancel_Transfer_Click" Width="100" HorizontalAlignment="Right" DockPanel.Dock="Bottom" IsEnabled="{Binding IsRunning}" Margin="0,0,0,0"/>
            </DockPanel>
            <ListBox ItemContainerStyle="{StaticResource ListBoxItemCommandStyle}" ItemsSource="{Binding OhtCommandList}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Height="60" TextElement.FontSize="18" CornerRadius="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="1" Text="{Binding SourceStation}" HorizontalAlignment="Left" VerticalAlignment="Center" FontFamily="Verdana"/>
                                <TextBlock Grid.Column="2" Text="{Binding DestStation}" HorizontalAlignment="Left" VerticalAlignment="Center" FontFamily="Verdana"/>
                                <TextBlock Grid.Column="3" Text="{Binding DurationSecond,StringFormat=0s}" HorizontalAlignment="Left" VerticalAlignment="Center" FontFamily="Verdana"/>
                                
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Grid>
</ui:Page>
