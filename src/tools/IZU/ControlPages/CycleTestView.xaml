<ui:Page
    x:Class="OHTC.Tools.ControlPages.CycleTestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="clr-namespace:OHTC.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sc="clr-namespace:SamplesCommon"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.Resources>
        <app:BooleanReverseConverter x:Key="BooleanReverseConverter" />
        <Style TargetType="ui:SimpleStackPanel">
            <Setter Property="Spacing" Value="12" />
        </Style>
    </Page.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
            <TextBlock Grid.Row="0" Text="OHT  Task  Overview" Margin="0,0,0,0" FontSize="18" Opacity="0.6" FontWeight="Bold"/>
        </Grid>
        <Grid Grid.Column="0" Grid.Row="1">
            <ListBox ItemContainerStyle="{StaticResource ListBoxItemCommandStyle}" ItemsSource="{Binding Ohts}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Border Height="260" Width="260" HorizontalAlignment="Left" BorderBrush="Gray" BorderThickness="1" Margin="10">
                                <StackPanel Margin="10">
                                    <TextBlock VerticalAlignment="Center" FontWeight="Bold" FontFamily="Arial Black" FontSize="20" Margin="0,10,0,10">
                                        <Run Text="{Binding OHTId}"/>
                                        <Run Text="("/>
                                        <Run Text="{Binding Status}"/>
                                        <Run Text=")"/>
                                    </TextBlock>
                                    <TextBox x:Name="tb_from_station" Width="auto" Text="{Binding CurrentTask.FromStation}" ui:ControlHelper.Header="Departure Station" Margin="0,10,0,0" IsReadOnly="True"/>
                                    <TextBox x:Name="tb_to_station" Width="auto" Text="{Binding CurrentTask.ToStation}" ui:ControlHelper.Header="Arrival Station" Margin="0,10,0,0" IsReadOnly="True"/>
                                    <UniformGrid Rows="0" Columns="2" Margin="0,10,0,0">
                                        <ui:AppBarButton Label="Stop" HorizontalAlignment="Left" Click="Stop_Clicked" Margin="0,0,0,0">
                                            <ui:AppBarButton.Icon>
                                                <ui:FontIcon Glyph="&#xE15b;" />
                                            </ui:AppBarButton.Icon>
                                        </ui:AppBarButton>
                                        <ui:AppBarButton Label="Start" HorizontalAlignment="Right" Click="Start_Clicked" Margin="0,0,0,0">
                                            <ui:AppBarButton.Icon>
                                                <ui:FontIcon Glyph="&#xE102;" />
                                            </ui:AppBarButton.Icon>
                                        </ui:AppBarButton>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="0,0,60,0"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
            <ui:AppBarButton Label="Settings" HorizontalAlignment="Right" Click="Schedule_Clicked" Margin="0,0,0,0">
                <ui:AppBarButton.Icon>
                    <ui:FontIcon Glyph="&#xE15E;" />
                </ui:AppBarButton.Icon>
            </ui:AppBarButton>
        </Grid>
    </Grid>
</ui:Page>
