<ui:Page
    x:Class="OHTC.Tools.ControlPages.CycleConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="clr-namespace:OHTC.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sc="clr-namespace:SamplesCommon"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    d:DesignHeight="500"
    d:DesignWidth="600"
    Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <app:BooleanReverseConverter x:Key="BooleanReverseConverter" />
        <Style TargetType="HeaderedContentControl">
            <Setter Property="Margin" Value="0,0,0,28" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock
                            Margin="0,0,0,16"
                            Style="{StaticResource BaseTextBlockStyle}"
                            Text="{Binding}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ui:SimpleStackPanel">
            <Setter Property="Spacing" Value="12" />
        </Style>
    </Page.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid
            Margin="0,40,5,0"
            DataContext="{Binding CurrentCommand}"
            TextElement.FontSize="16">
            <ui:AppBarButton
                Width="42"
                Height="42"
                Margin="0,0,0,0"
                HorizontalAlignment="Right"
                Click="Reload_Click">
                <ui:AppBarButton.Icon>
                    <ui:FontIcon Glyph="&#xE117;" />
                </ui:AppBarButton.Icon>
            </ui:AppBarButton>
            <HeaderedContentControl Margin="0,30,0,0" Header="Station">
                <ui:SimpleStackPanel>
                    <DockPanel>
                        <TextBlock
                            Width="120"
                            VerticalAlignment="Center"
                            Text="From Station" />
                        <TextBox
                            x:Name="tb_from"
                            Width="180"
                            Margin="10,0,0,0" />
                    </DockPanel>
                    <DockPanel Margin="0,10,0,0">
                        <TextBlock
                            Width="120"
                            VerticalAlignment="Center"
                            Text="To Station" />
                        <TextBox
                            x:Name="tb_to"
                            Width="180"
                            Margin="10,0,0,0" />
                    </DockPanel>
                    <DockPanel Margin="0,10,0,0">
                        <TextBlock
                            Width="120"
                            VerticalAlignment="Center"
                            Text="OHT:" />
                        <TextBox
                            x:Name="OHT_ID"
                            Width="180"
                            Margin="10,0,0,0" />
                    </DockPanel>
                    <DockPanel Margin="0,10,0,0">
                        <TextBlock
                            Width="120"
                            VerticalAlignment="Center"
                            Text="Duration" />
                        <TextBlock
                            Width="20"
                            Margin="0,0,0,0"
                            VerticalAlignment="Center"
                            DockPanel.Dock="Right"
                            Text="s" />
                        <TextBox
                            x:Name="tb_sec"
                            Width="180"
                            Margin="30,0,0,0" />
                    </DockPanel>
                    <Button
                        Width="100"
                        Height="60"
                        Margin="0,30,0,0"
                        HorizontalAlignment="Left"
                        Click="Add_Click"
                        Content="  Add Task  "
                        IsEnabled="{Binding IsRunning, Converter={StaticResource BooleanReverseConverter}}" />
                </ui:SimpleStackPanel>
            </HeaderedContentControl>
        </Grid>
        <DockPanel Grid.Column="1" Margin="5,0,0,0">
            <ListBox
                x:Name="lb"
                ItemContainerStyle="{StaticResource ListBoxItemCommandStyle}"
                ItemsSource="{Binding OhtCommandList}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Height="60"
                            CornerRadius="2"
                            TextElement.FontSize="18">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="0.2*" />
                                    <ColumnDefinition Width="60" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontFamily="Verdana"
                                    Text="{Binding SourceStation}" />
                                <TextBlock
                                    Grid.Column="2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontFamily="Verdana"
                                    Text="{Binding DestStation}" />
                                <TextBlock
                                    Grid.Column="3"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontFamily="Verdana"
                                    Text="{Binding DurationSecond, StringFormat=0s}" />
                                <ui:AppBarButton
                                    Grid.Column="4"
                                    VerticalAlignment="Center"
                                    Click="Delete_Click"
                                    Label="Delete">
                                    <ui:AppBarButton.Icon>
                                        <ui:FontIcon Glyph="&#xE107;" />
                                    </ui:AppBarButton.Icon>
                                </ui:AppBarButton>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Grid>
</ui:Page>
