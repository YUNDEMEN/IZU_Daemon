<ui:Page
    x:Class="OHTC.Tools.ControlPages.IZUOverView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="clr-namespace:OHTC.Tools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sc="clr-namespace:SamplesCommon"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Page.Resources>
        <sc:ListBoxTemplateSelector x:Key="ListBoxTemplateSelector"/>
        <app:BooleanReverseConverter x:Key="BooleanReverseConverter" />
        <Style TargetType="ui:SimpleStackPanel">
            <Setter Property="Spacing" Value="12" />
        </Style>
    </Page.Resources>
    <Grid Margin="10">
        <DockPanel>
            <TextBlock Text="IZU List" Margin="0,0,0,10" FontSize="18" Opacity="0.6" FontWeight="Bold" DockPanel.Dock="Top"/>
            <ListBox x:Name="izu_list" MouseDoubleClick="izu_list_MouseDoubleClick"
                ItemContainerStyle="{StaticResource ListBoxItemCommandStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="100" Width="200" HorizontalAlignment="Left">
                            <TextBlock Text="{Binding}" FontSize="16" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Margin="0,5,0,0"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </DockPanel>
        <ContentControl x:Name="izu_content"/>
    </Grid>
</ui:Page>
